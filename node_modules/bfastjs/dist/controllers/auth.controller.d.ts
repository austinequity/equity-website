import { AuthAdapter, AuthOptions } from "../adapters/auth.adapter";
import { UserModel } from "../models/UserModel";
import { CacheController } from "./cache.controller";
export declare class AuthController {
    private readonly appName;
    private readonly cacheController;
    private readonly authAdapter;
    constructor(appName: string, cacheController: CacheController, authAdapter: AuthAdapter);
    authenticated(userId: string, options?: AuthOptions): Promise<any>;
    currentUser(): Promise<any>;
    getEmail(): Promise<any>;
    getToken(): Promise<any>;
    getUsername(): Promise<any>;
    logIn(username: string, password: string, dtl?: number, options?: AuthOptions): Promise<any>;
    logOut(options?: AuthOptions): Promise<boolean>;
    requestPasswordReset<T extends UserModel>(email: string, options?: AuthOptions): Promise<any>;
    signUp(username: string, password: string, attrs?: {
        [key: string]: any;
    }, dtl?: number, options?: AuthOptions): Promise<any>;
    updateUser(userId: string, attrs?: {
        [key: string]: any;
    }, options?: AuthOptions): Promise<any>;
    setCurrentUser(user: {
        [key: string]: any;
    } | null, dtl?: number): Promise<any>;
    requestEmailVerification(email: string, options?: AuthOptions): Promise<any>;
}
