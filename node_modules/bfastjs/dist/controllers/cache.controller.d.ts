import { CacheAdapter } from "../adapters/cache.adapter";
export declare class CacheController {
    private readonly appName;
    private readonly database;
    private readonly collection;
    private readonly cacheAdapter;
    constructor(appName: string, database: string, collection: string, cacheAdapter: CacheAdapter);
    keys(): Promise<string[]>;
    clearAll(): Promise<boolean>;
    get<T>(identifier: string, options?: {
        secure?: boolean;
    }): Promise<T>;
    getAll(): Promise<Array<any>>;
    query(filter: (element: {
        [key: string]: any;
    }[]) => any): Promise<any>;
    set<T>(identifier: string, data: T, options?: {
        dtl?: number;
        secure?: boolean;
    }): Promise<T>;
    remove(identifier: string, force?: boolean): Promise<boolean>;
}
